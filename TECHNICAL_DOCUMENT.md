# 课程智航 CoursePilot —— 技术文档

> 本文档严格根据当前已实现的代码撰写，不包含任何未实现的功能。

---

## 1. 项目概述

CoursePilot（课程智航）是一个面向大学课程资料的**任务型 AI Agent**。用户上传一份课程资料（PDF/TXT/Markdown），并用一句自然语言描述自己的学习目标，系统会自动理解任务、选择合适的工具（Tool）、读取课程资料、调用大模型生成结果，并把整个执行过程展示出来。

项目定位为**校赛答辩用 MVP**：技术栈精简（Python + Streamlit + 一次 HTTP 调用即可对接的 LLM 接口），没有引入数据库、向量库、多智能体框架等重型组件，核心目标是"稳定可运行、可现场演示"。

## 2. 项目背景

目前大学生使用 AI 工具复习备考，大多停留在"把资料丢给 Chatbot，然后一句一句问"的模式，用户仍然要自己完成"我该做什么任务、该怎么拆解"这部分认知工作。CoursePilot 希望验证一个更进一步的形态：**Agent 自己判断用户想做什么任务，并自主选择合适的工具去完成它**，而不是让用户在多个功能按钮之间手动切换。

## 3. 用户需求

- 上传一份课程资料后，能够针对同一份资料完成三类完全不同的诉求：知识点问答、复习计划制定、练习题生成；
- 不希望在界面上手动点选"我要用哪个功能"，而是直接说出目标，由系统判断；
- 需要清楚地知道系统是"真的理解了任务并做了检索/推理"，而不是随机套模板；
- 在网络不稳定或没有配置大模型 Key 的情况下，系统也不能直接崩溃或无法使用。

## 4. 系统总体架构

```
┌─────────────────────────────┐
│           app.py             │  Streamlit UI：上传资料 / 输入目标 / 展示轨迹与结果
└───────────────┬──────────────┘
                │
                ▼
┌─────────────────────────────┐
│      agent/router.py         │  Agent Router：任务分类 + run_agent 执行主流程
└───────────────┬──────────────┘
                │ 根据分类结果调用
                ▼
┌─────────────────────────────┐
│       agent/tools.py         │  三个 Tool：document_search / study_plan / quiz_generator
└───────┬───────────────┬─────┘
        │               │
        ▼               ▼
┌───────────────┐ ┌───────────────┐
│ document/      │ │ utils/llm.py   │
│ parser.py      │ │ (LLM 调用封装) │
│ 资料解析+切章节 │ │                │
└───────────────┘ └───────────────┘
```

各模块职责单一、依赖方向单向（`app.py` → `agent` → `document`/`utils`），没有循环依赖，便于在比赛前快速定位问题。

## 5. Agent 工作流程

以用户输入"根据这份计算机网络课程资料，帮我制定今晚的考试复习计划"为例，`agent/router.py` 中 `run_agent()` 的真实执行步骤为：

1. **接收到用户任务** —— 拿到 `app.py` 传入的原始任务字符串；
2. **分析任务类型** —— 准备进行任务分类；
3. **选择 Tool** —— 调用 `select_tool()`，得到工具名与判断依据（例如"大模型判断该任务属于 study_plan"或"关键词规则命中『复习计划』"）；
4. **执行对应 Tool** —— 调用 `tools.study_plan()` / `tools.document_search()` / `tools.quiz_generator()`；
5. 该 Tool 内部会依次记录：**检索课程资料**（关键词匹配到哪些章节）→ **获取相关课程内容**（提取了多少字的上下文）→ **调用大模型生成结果**（或说明降级为简化模式的原因）；
6. **完成任务** —— 所有步骤记录写入 `trace` 列表并返回给 UI。

这个 `trace` 列表是**代码真实运行过程中逐步 `append` 出来的日志**，`app.py` 通过 `on_step` 回调把每一步实时渲染到页面上，不存在预先写死的"假装执行"文案。

## 6. Agent Router 设计

Router 的核心函数是 `agent/router.py` 中的 `select_tool(user_task)`，采用**"大模型判断 + 关键词规则兜底"**的双层策略：

1. **大模型判断（主路径）**：如果检测到已配置 `LLM_API_KEY`，会构造一个分类 Prompt，要求大模型仅从 `document_search` / `study_plan` / `quiz_generator` 三个标识符中选择一个输出。这是真正意义上"由 Agent 自主理解自然语言意图"的部分，用户可以用任意措辞描述任务，不需要命中固定句式。
2. **关键词规则（兜底路径）**：当未配置大模型、大模型调用失败、或返回内容不合法（不包含三者之一）时，自动降级为关键词规则匹配（如命中"复习计划""选择题"等词），保证 Router 在任何网络状况下都有确定性的输出，不会卡死或抛异常。

无论走哪条路径，`select_tool()` 都会返回 `(tool_name, reason)`，`reason` 会在 UI 上原样展示，方便评委看到"为什么 Agent 选择了这个工具"。

## 7. Tool 设计

三个 Tool 都实现在 `agent/tools.py`，遵循统一模式：**关键词检索定位相关章节 → 有大模型则调用大模型生成 → 无大模型或调用失败则自动降级为基于原文的简化处理**。

- **`document_search(query, sections)`**：用 `search_relevant_sections()` 对课程资料做关键词打分（中文按 2-gram 切词、英文按单词切分，统一转小写避免大小写不匹配），取分数最高的 3 个章节作为上下文，交给大模型回答；若无大模型，则直接展示匹配到的原文片段。返回结果包含 `answer`、`sources`（引用章节）、`context_used`（实际使用的上下文）。

- **`study_plan(goal, sections)`**：把课程资料的章节标题与摘要整理成大纲，连同用户目标一起交给大模型，要求生成带具体时间段的复习计划并指出优先级；若无大模型，则按章节数量均分固定总时长（150 分钟，从 19:00 开始），生成模板计划。

- **`quiz_generator(topic_hint, sections, num=5)`**：先通过 `_title_matches_hint()` 判断用户提到的章节名称（如"TCP/IP"）命中哪些章节，命中失败则退化为关键词检索；把选中章节的原文交给大模型，要求严格输出 JSON 格式的题目数组（题干/选项/答案/解析），并做 JSON 解析与代码块符号清洗，解析失败时展示原始文本兜底；若无大模型，则用 `_fallback_quiz()` 从原文中抽取真实句子构造"以下哪一项描述符合资料内容"的选择题，干扰项来自其他章节的真实句子，保证即使没有大模型也能出"有据可依"的题目。

每个 Tool 返回结果中都包含 `llm_used`（布尔值）与 `detail_log`（该 Tool 内部真实执行的步骤说明），供 Router 和 UI 复用。

## 8. 课程资料处理

`document/parser.py` 负责把资料变成 Agent 可用的结构化数据：

- **格式解析**：
  - TXT / Markdown：按 UTF-8 → GBK 顺序尝试解码，避免中文 Windows 用户常见的编码问题；
  - PDF：使用 `pypdf` 逐页 `extract_text()`；如果提取结果为空（例如扫描版/图片版 PDF），会返回明确的错误提示，而不是静默失败或让程序崩溃；
- **章节切分**（`split_into_sections`）：按 Markdown 的 `##`~`####` 标题切分为章节列表，一级标题 `#` 视为文档总标题而不单独成章，避免"标题+简介"污染章节列表；如果资料完全没有标题（例如从 PDF/TXT 提取出的纯文本），则退化为按空行分段，每段生成编号标题（"片段 1"、"片段 2" ...），保证下游逻辑始终有章节可用。

这一层是"课程资料进入 Agent"的入口：无论用户上传什么格式，最终都会变成统一的 `[{"title": ..., "content": ...}, ...]` 结构，供 Router 和三个 Tool 使用，而不需要额外引入向量数据库。

## 9. LLM 调用

`utils/llm.py` 封装了一个通用的 OpenAI 风格 `/chat/completions` 调用：

- 配置通过 `.env`（`LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL`）读取，兼容 OpenAI、DeepSeek、Moonshot、智谱 GLM-4 等任何提供该风格接口的服务；
- `is_configured()` 用于判断是否已配置 Key，供 Router 和各 Tool 在运行前判断走"大模型路径"还是"简化模式"；
- `chat()` 对以下异常情况做了显式处理，全部返回结构化的 `LLMResult(success, content, error)`，不会抛出未捕获异常：
  - 未配置 API Key；
  - 网络超时（`Timeout`）；
  - 连接失败（`ConnectionError`）；
  - HTTP 状态码非 200（附带服务端返回内容便于排查）；
  - 返回体不是合法 JSON，或缺少 `choices[0].message.content` 字段；
  - 返回内容为空字符串。

这保证了"没有 API Key，或比赛现场网络抖动导致大模型调用失败"都不会让整个 Demo 中断，而是自然降级为简化模式并在 UI 上提示原因。

## 10. UI 设计

`app.py` 使用 Streamlit 构建单页应用，结构与需求一一对应：

1. 顶部展示项目名与副标题；
2. 【课程资料】区：文件上传 + "使用内置演示资料"按钮，加载后展示资料字数、识别到的章节目录；
3. 【学习目标】区：自然语言输入框 + 三个一键填充的示例任务按钮（对应三个现场 Demo 场景）；
4. 【开始执行】按钮触发 `run_agent()`，并通过 `on_step` 回调把每一步执行日志实时追加渲染到"Agent 执行过程"区域；
5. 【任务结果】区：根据实际选中的 Tool 展示对应格式的结果（问答/计划/题目），并注明本次结果来自"大模型生成"还是"简化模式生成"；
6. 侧边栏展示当前大模型配置状态，便于评委确认系统的真实运行模式。

## 11. Demo 测试

项目内置 `data/demo_course.md`（模拟《计算机网络》课程资料，覆盖 OSI、TCP/IP、TCP 三次握手、HTTP、DNS、网络安全基础六大知识点），并针对赛题要求的三个场景做过手动联调：

| Demo | 输入任务 | 预期 Tool | 实测结果 |
| --- | --- | --- | --- |
| Demo 1 | "帮我制定今晚的考试复习计划……" | `study_plan` | 命中关键词"复习计划"，生成覆盖全部章节的时间表 |
| Demo 2 | "根据 TCP/IP 这一章生成5道选择题……" | `quiz_generator` | 命中标题关键词"TCP"，定位到"TCP/IP 协议族"与"TCP 三次握手"两章并出题 |
| Demo 3 | "解释 TCP 三次握手……" | `document_search` | 关键词打分命中"TCP 三次握手"章节并给出解答 |

此外 `tests/test_basic.py` 覆盖了文档解析（含无标题文本、不支持的文件类型）、Router 分类（大模型可用/不可用两种路径）、三个 Tool 的基本调用（含大模型返回被 `monkeypatch` 模拟的情况），全部 15 个用例本地执行通过，且不依赖真实网络请求。

## 12. 项目创新点

- **真正的任务路由，而非写死的按钮**：Agent 通过大模型理解自然语言意图来选择工具，用户可以用任意措辞提出需求；
- **可解释的执行轨迹**：每一步 Agent 决策（为什么选这个 Tool、检索到了哪些章节、是否调用了大模型）都被记录并展示，而不是把大模型输出当黑盒直接甩给用户；
- **无大模型也能跑通全流程的降级设计**：三个 Tool 都有基于原文的确定性兜底逻辑（原文展示 / 模板计划 / 原文抽取式选择题），这既是工程稳健性设计，也让"Agent 的检索与决策能力"和"底层大模型能力"解耦，便于现场演示排除网络风险。

## 13. 应用价值

- 帮助学生把"零散的课程资料"直接转化为"可执行的复习动作"（问答、计划、习题），减少在多个工具间来回切换的成本；
- Router + Tool 的架构可以低成本扩展到更多学习场景（如生成思维导图、错题总结等），只需新增一个 Tool 函数并让 Router 能够识别即可；
- 轻量级技术栈（无数据库、无向量库、无复杂框架）证明"任务型 Agent"不依赖复杂基础设施也能落地，对同类学生项目有参考价值。

## 14. 当前不足

- 章节检索使用的是关键词打分（中文 2-gram + 英文单词），而非语义向量检索，对于表述差异较大、不与原文共享关键词的提问，召回效果有限；
- Router 的关键词兜底规则集是手工列举的有限词表，覆盖不到的措辞在无大模型时会默认落到 `document_search`；
- `study_plan` 的时间安排（默认从 19:00 开始、总时长 150 分钟）在无大模型时是固定模板，不能根据用户输入的具体起止时间自动调整；
- 未做任何持久化，刷新页面后已上传资料和历史结果会丢失，这是为了保持 MVP 足够简单而做出的取舍。

## 15. 后续展望

- 引入轻量的语义相似度（例如本地小型 embedding 模型）替代关键词打分，提升检索准确率，但仍避免引入重型向量数据库；
- 让 `study_plan` 从用户输入中解析具体的时间窗口（如"19:00-21:30"），而不是使用固定默认值；
- 扩展更多 Tool（如生成知识点思维导图、根据错题生成同类题），验证 Router+Tool 架构的可扩展性；
- 增加多轮对话能力，让用户可以基于上一次结果继续追问或要求调整（例如"再出5道难一点的题"）。
